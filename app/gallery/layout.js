export const metadata = {
  title: "Gallery",
  description:
    "Photo albums from Mother Language Festivals, International Mother Language Day, and MLLWS community events.",
};

export default function GalleryLayout({ children }) {
  return children;
}
