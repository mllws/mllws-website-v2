export const metadata = {
  title: "Stories",
  description:
    "Recaps, milestones, and the history behind International Mother Language Day from MLLWS.",
};

export default function StoriesLayout({ children }) {
  return children;
}
